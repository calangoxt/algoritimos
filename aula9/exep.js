function soma(a,b){
return a+b;//deve ser colocado na ultima linha da funcao 
}
let resultado = soma(3, 4)
console.log(resultado + 1)

const calcularArea = function(altura, largura){
    const area = altura * largura 
    return area
}
const areaCalculada = calcularArea(7, 6)

const calcularArea2 = (altura2, largura2) => {
    const area2 = altura2 * largura2 
    return area2
}
const areaCalculada2 = calcularArea2(5, 7)


